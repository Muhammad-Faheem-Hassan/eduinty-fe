import { Typography } from "@mui/material";

const Dashboard = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Welcome to Dashboard
      </Typography>
      <Typography>
        This layout is fully responsive with a sidebar and topbar.
      </Typography>
    </div>
  );
};

export default Dashboard;
